export class UserProfile {
    constructor (profilePicture, phoneNumber, address){
        this.profilePicture = profilePicture;
        this.phoneNumber = phoneNumber;
        this.address = address;
    }
}